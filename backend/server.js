const app = require('./app')

PORT = 8686

app.listen(PORT, () => {

    console.log(`Server running on ${PORT}`)
    console.log(`Server running on ${PORT}`)
    console.log(`Server running on ${PORT}`)
    console.log(`Server running on ${PORT}`)
    console.log(`Server running on ${PORT}`)
    console.log(`Server running on ${PORT}`)

})